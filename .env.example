# Visible app name (shown in the browser title).
VITE_APP_NAME="Vue Starter"

# Base URL of the API. The SPA appends /api automatically.
VITE_API_URL=http://localhost:8001

# Sentry — leave empty in development to avoid reporting.
VITE_SENTRY_DSN=
